[project]
name = "gerador-qr-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Robson R Junior", email = "robson.rjunior34@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pillow>=12.3.0",
    "qrcode>=8.2",
]

[project.scripts]
gerador-qr-code = "gerador_qr_code:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
